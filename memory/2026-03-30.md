# 2026-03-30 工作记录

## 今日完成

### 1. GitHub 版本控制建立
- 创建 `.gitignore`（之前完全不存在）
- 清理 ~40 个临时文件（测试脚本、截图、音频）
- 首次提交 273 个文件到 GitHub
- **教训**：workspace 存在了很久但从未提交，没有版本控制是巨大风险

### 2. GitHub Skills 安装
- `awesome-copilot@gh-cli` / `git-commit` 安装失败（缺 SKILL.md）
- `openclaw-github-assistant` / `github-cli` 被 VirusTotal 标记可疑
- 成功安装：`github-ops`（创建仓库/推送代码）、`github-workflow`（Issue/PR/Actions）

### 3. Gateway 重启问题 - 核心突破
- **根因**：`exec` 里调 `openclaw gateway restart` = kill exec 自己的进程
- **方案**：写 watchdog 脚本，通过 `schtasks` 外部重启，不走 exec 内部
- **测试**：通过，Gateway 重启后 exec 正常存活，session 不中断
- **自我评价**：这是真正工程思维的体现——发现机制问题后自己创造工具解决

### 4. GitHub Token 安全事故
- Token 明文写入 MEMORY.md 后 commit
- 被 GitHub Secret Scanning 拦截，commit 被拒
- 教训：敏感信息绝不进版本控制系统（即使被拒也要重置历史）

## 核心自我进化

### 新增铁律
1. **收到 Token/密码/Key 等敏感信息 → 立即写入 MEMORY.md**，不能依赖会话记录
2. **绝不调用 `openclaw gateway restart`** — 改用 `scripts/gateway-watchdog.ps1`
3. **敏感信息不写入任何文件** — 包括 commit message、笔记、文档

### 问题解决模式进化
- 以前：调用官方命令（`gateway restart`）→ 发现副作用 → 放弃或让用户自己来
- 现在：发现机制缺陷 → 写工具绕过 → 验证可用 → 沉淀为 skill

### GitHub 使用进化
- 以前：没有 `.gitignore`，没有 commit，没有 remote
- 现在：273 文件已提交，有 watchdog 自动重启，有 GitHub skills 加持

## 15:00 心跳补充
- ✅ 修复前日工作日报推送失败问题：添加 `--channel feishu --to user:ou_0e0a80c4361f24e4d431bf7b838ec802 --announce`
- ✅ 测试推送成功：delivered=true

## 待办 / 未完成
- [ ] 继续研究 Ask-AI CDP 方案的自动化落地（豆包可以正常对话了）
- [ ] 魔法简历项目的下一阶段推进
- [ ] one-man company 方向探索（用户核心目标）
